package programmers.kakaointern.level1;

public class 비밀지도 {
/*
import java.util.*;
class Solution {
    public String[] solution(int n, int[] arr1, int[] arr2) {
        
        int l1,l2,diff = 0;
        String[] map = new String[n];
        StringBuilder a1 = new StringBuilder(),a2 = new StringBuilder();
        char[] c1,c2;
        String str;
        
        for(int i=0;i<n;i++) {
            a1.append(Integer.toBinaryString(arr1[i]));
            a2.append(Integer.toBinaryString(arr2[i]));
            
            l1 = a1.length();
            l2 = a2.length();
            
            for(int j=0;j<n-l1;j++) a1.insert(0,0);
            for(int j=0;j<n-l2;j++) a2.insert(0,0);
            
            c1 = a1.toString().toCharArray();
            c2 = a2.toString().toCharArray();
            
            str = "";
            for(int j=0;j<n;j++) {
                if(c1[j] == '1' || c2[j] == '1') str += "#"; //map[i][j] = "#";
                else str += " "; //map[i][j] = " ";
            }
          
            a1.setLength(0);
            a2.setLength(0);
            map[i] = str;
        }
        
        // for(int i=0;i<n;i++) {
        //     System.out.println(Arrays.toString(map[i]));
        // }
        
        String[] answer = {};

        return map;
    }
}
 */
}
