package programmers.kakaointern.level2;

public class 튜플 {
/*
import java.util.*;
class Solution {
    public int[] solution(String s) {
        
        HashMap<String,Integer> hm = new HashMap<>();
        int len = s.length();
        String arr[] = s.substring(2,len-2).replace("},{",",").split(",");
        
        for(String str : arr) hm.put(str,hm.getOrDefault(str,0)+1);
        
        List<Map.Entry<String,Integer>> entry = new ArrayList<>(hm.entrySet());
        entry.sort((s1,s2) -> s2.getValue() - s1.getValue());
        
        // System.out.println(entry);
        int[] answer = new int[hm.size()];
        int idx = 0;
        for(Map.Entry<String,Integer> map : entry) answer[idx++] = Integer.parseInt(map.getKey());
        return answer;
    }
}
 */
}
