package programmers.kakaointern.level2;

public class n진수게임 {
/*
import java.util.*;
class Solution {
    public String solution(int n, int t, int m, int p) {
        
        int size = t * m * p;
        String str = "";
        int len = 0, idx =0;
        StringBuilder sb = new StringBuilder();
        
        while(size >= len) {
            sb.append(Integer.toString(idx++,n));
            len = sb.length();
            // System.out.println("[idx,str]: ["+idx+","+str+"]");
        }
        
        str = sb.toString().toUpperCase();
        // System.out.println("str: "+str);
        
        String answer = "";
        for(int i=0;i<t;i++){
            answer += str.charAt((p-1)+m*i);
        }
        
        return answer;
    }
    
}
 */
}
