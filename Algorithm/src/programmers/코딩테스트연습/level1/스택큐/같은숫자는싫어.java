package programmers.코딩테스트연습.level1.스택큐;

public class 같은숫자는싫어 {
/*
import java.util.*;

public class Solution {
    public int[] solution(int []arr) {
        
        Deque<Integer> dq = new ArrayDeque<>();
        for(int cur : arr) {
            if(dq.isEmpty()) dq.push(cur);
            else if(dq.peek() != cur) dq.push(cur);
        }
        
        int len = dq.size();
        int[] answer = new int[len];
        for(int i=0;i<len;i++) answer[i] = dq.pollLast();

        return answer;
    }
}
 */
}
