package programmers.코딩테스트연습.level1.완전탐색;

public class 최소직사각형 {
/*
import java.util.*;
class Solution {
    public int solution(int[][] sizes) {
        
        int max, min, garo = 0, sero = 0;
        for(int[] size : sizes){
            max = Math.max(size[0],size[1]);
            min = Math.min(size[0],size[1]);
            
            garo = Math.max(max,garo);
            sero = Math.max(min,sero);
        }
        
        return garo * sero;
    }
}
 */
}
