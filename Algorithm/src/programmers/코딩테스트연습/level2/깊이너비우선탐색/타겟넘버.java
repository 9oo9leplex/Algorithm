package programmers.코딩테스트연습.level2.깊이너비우선탐색;

public class 타겟넘버 {
/*
class Solution {
    
    static int answer;
    
    public int solution(int[] numbers, int target) {
        
        int len = numbers.length;
        answer = 0;
        
        dfs(0,0,len,target,numbers);
        
        return answer;
    }
    
    private static void dfs(int cnt, int sum, int limit, int target,int[] numbers){
        
        if(cnt == limit) {
            if( sum == target) answer++;
            return;
        }
        
        dfs(cnt+1,sum+numbers[cnt],limit,target,numbers);
        dfs(cnt+1,sum-numbers[cnt],limit,target,numbers);
    }
    //
}
 */
}
