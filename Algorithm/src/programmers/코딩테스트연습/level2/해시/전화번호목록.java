package programmers.코딩테스트연습.level2.해시;

public class 전화번호목록 {
/*
import java.util.*;
class Solution {
    public boolean solution(String[] phone_book) {
        
        HashMap<String, Integer> hm = new HashMap<>();
        char c;
        int idx=0;
        
        for(String str : phone_book) hm.put(str,idx++);
        

        int len = phone_book.length;
        for(int i=0;i<len;i++){
            idx = phone_book[i].length();
            for(int j=1;j<idx;j++){
                // System.out.println(phone_book[i]);
                if(hm.containsKey(phone_book[i].substring(0,j))) return false;
            }
        }
        return true;
    }
}
 */
}
