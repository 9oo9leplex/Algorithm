package programmers.코딩테스트연습.level2.힙;

public class 더맵게 {
/*
import java.util.*;
class Solution {
    public int solution(int[] s, int K) {
        int answer = -1, first = 0,second;
        PriorityQueue<Integer> pq = new PriorityQueue<>();
        for(int c : s) pq.add(c);
        
        while(true){
            first = pq.poll();
            if(first >= K) return answer+1;
            else if(pq.size() == 0) break;
            second = pq.poll();
            first += second *2;
            pq.add(first);
            answer++;
        }
        
        return -1;
    }
}
 */
}
